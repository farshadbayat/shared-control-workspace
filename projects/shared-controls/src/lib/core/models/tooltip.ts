import { TooltipPosition } from '@angular/material/tooltip';

export interface Tooltipe {
    tooltipText?: any;
    tooltipClass?: any;
    tooltipShowDelay?: number;
    tooltipPosition?: TooltipPosition;
    tooltipMessage?: string;
    tooltiphideDelay?: number;
    tooltipDisabled?: boolean;
}
